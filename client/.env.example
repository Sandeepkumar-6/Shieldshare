# ShieldShare client configuration. Copy to client/.env.
# The client only ever receives VITE_API_URL. Never put a secret in a VITE_ variable:
# everything prefixed VITE_ is bundled into public JavaScript.
VITE_API_URL=http://localhost:5000
